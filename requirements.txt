# Web framework
fastapi==0.110.0
uvicorn[standard]==0.23.2
pydantic-settings==2.2.1
mangum>=0.17.0,<0.18

# AWS SDK
boto3>=1.28.0,<1.32.0

# Database
# psycopg2-binary 2.9.9 fails to build on Python 3.14 in this environment.
# Use psycopg v3 binary package on 3.14+, keep psycopg2-binary for older interpreters.
psycopg2-binary==2.9.9; python_version < "3.14"
psycopg[binary]>=3.2.1,<3.3; python_version >= "3.14"
SQLAlchemy==2.0.41
alembic==1.14.0  # for migrations

# Auth / JWT
python-jose==3.3.0

# HTTP / API calls
httpx==0.26.0

# Caching / Utility
python-dotenv==1.0.0

# Optional for testing
pytest==8.2.0
pytest-asyncio==0.22.0
pre-commit==3.8.0
